{% extends 'base.html.twig' %}

{% block title %}New home{% endblock %}

{% block body %}
    <h1>Create new home</h1>

    {{ include('home/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('home_index') }}">back to list</a>
{% endblock %}
